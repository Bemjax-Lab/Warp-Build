The pdf holds most of the knowledge this engine is based on

The `parametersModel.json` comes from existing POC app we will integrate this engine in to. The app uses these parameters fro localisation. When Localisation is made it will use the results to pass it to this engine to get diff diagnoses.