Dario 2026-04-30 - Ivana sent a parametersModel change list (testing-driven). Logged as a new ToDo section "Parameters model — Ivana 2026-04-30". Awaiting Master confirmation before applying.
Dario 2026-04-30 - Master confirmed; applied. parametersModel.json edits: onset += 'intermittent'; behavior 'compulsive' → 'compulsive / head pressing'; gait += 'generalised weakness', 'rolling L', 'rolling R', 'circling L', 'circling R' (replacing plain 'circling'), 'hypermetria L', 'hypermetria R' (replacing plain 'hypermetria'); bodyPosture += 'neck ventroflexion', removed 'plantigrade RT/LT' and 'palmigrade RP/LP' (palmigrade thoracic-only, plantigrade pelvic-only); cranial nerves: removed facialSymmetry + facialAsymmetryLocation, added facialParesis (multi-select L/R), added blindness yes/no; tongue: removed ventral/dorsal atrophy values, new tongueAtrophy param (multi-select L/R); muscleAtrophy += 'generalized'; muscleAtrophyLocation values changed to TR/TL/PR/PL multi-select; new "systemic signs" category with fecalIncontinence, urinaryIncontinence, femoralPulse.
Dario 2026-04-30 - Cross-cutting refactor (Python regex pass): rules/* + diagnoses/* + ddxHelpers.js patched — `hasValue(_, 'gait', 'circling')` → `(hasValue(_, 'gait', 'circling L') || hasValue(_, 'gait', 'circling R'))`; same pattern for hypermetria; `_.facialSymmetry === 'asymmetric'` → `(hasValue(_, 'facialParesis', 'L') || hasValue(_, 'facialParesis', 'R'))`; `hasValue(_, 'tongue', 'ventral|dorsal tongue base atrophy')` → `(hasValue(_, 'tongueAtrophy', 'L') || hasValue(_, 'tongueAtrophy', 'R'))`; `behavior === 'compulsive'` → `behavior === 'compulsive / head pressing'`. 17 files patched. Manual fixes: midbrain.js ptosis check (was using facialAsymmetryLocation === 'eyelids' — now relies solely on palpebralClosure incomplete); diagnoses/brainstem.js facialSymmetry !== 'asymmetric' rewritten to !facialParesis.
Dario 2026-04-30 - rules/multifocal.js vestibularSigns() now also accepts 'rolling L'/'rolling R' (per Ivana: rolling = vestibular ataxia equivalent).
Dario 2026-04-30 - definedTests.json migrated: "circling" → "circling R" (side unknown — defaulted R), "hypermetria" → "hypermetria R" (same), "compulsive" behavior → "compulsive / head pressing", facialSymmetry/facialAsymmetryLocation → facialParesis: ["R"] (side unknown), tongue ventral/dorsal atrophy → tongueAtrophy: ["R"]. Sides defaulted to R because original tests had no side info — Ivana should review and correct any tests where side matters clinically.
Dario 2026-04-30 - Smoke test after migration: 30/45 pass. 15 failures expected — combination of the previously-known multifocal "alongside vs exclusive" doctrine mismatches (Ivana to update expected arrays or rule-side guards) plus possible side-default mismatches in migrated tests. Nothing crashed; no rule-level syntax errors. To be triaged with Ivana.
Dario 2026-04-30 - Bug caught: parametersModel and diagnosesParameters BOTH had a top-level "systemic signs" category. Parameters.init() merge was `self.model[cat] = diagModel[cat]` — overwriting, not merging. Result: Ivana's 3 new systemic params (fecal/urinary incontinence, femoral pulse) were silently shadowed by the diagnosis-side category (fever, weightLoss, anorexia, lethargy). Fixed merge to do `Object.assign` when category exists on both sides; both sets now visible in the Parameters UI.
